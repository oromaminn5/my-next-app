services:
  app:
    build: .
    ports:
      - "3000:3000"
    volumes:
      - type: bind
        source: .
        target: /app
    working_dir: /app
    command: sh -c "npm run dev"
    stdin_open: true
    tty: true
